#include <stdio.h>
int fact(int n)
{
if(n==0)
{
return 1;
}
else
{
return n*fact(n-1);
}
}
int main()
{
int n,f;
printf("Enter Number=");
scanf("%d",&n);
f=fact(n);
printf("\nFactorial=%d",f);
return 0;
}
